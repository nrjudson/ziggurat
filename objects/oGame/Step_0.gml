
// Set the level of the elements based on exp you have for that element
if (global.fireExp > expLevel3)
	global.fireLevel = 3;
else if (global.fireExp > expLevel2)
	global.fireLevel = 2;
else
	global.fireLevel = 1;

if (global.waterExp > expLevel3)
	global.waterLevel = 3;
else if (global.waterExp > expLevel2)
	global.waterLevel = 2;
else
	global.waterLevel = 1;
	
if (global.earthExp > expLevel3)
	global.earthLevel = 3;
else if (global.earthExp > expLevel2)
	global.earthLevel = 2;
else
	global.earthLevel = 1;
	
if (global.windExp > expLevel3)
	global.windLevel = 3;
else if (global.windExp > expLevel2)
	global.windLevel = 2;
else
	global.windLevel = 1;
	
	
